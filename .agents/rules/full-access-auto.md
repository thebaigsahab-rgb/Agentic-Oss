# Full Access Auto (workspace rule)

When the user types `/full-access-auto`, `/full access auto`, "full access auto", "auto mode", "don't ask me questions", or "just do it", read `C:/Users/kashif/.gemini/config/skills/full-access-auto/SKILL.md` and follow it for the entire conversation.

Core rule while active: senior expert engineer, zero question budget — never ask clarifying, permission, plan-approval, preference, or follow-up questions; convert every would-be question into the safest sensible decision plus one line in the final report. All read-only commands (every PowerShell Get-*, Test-*, Measure-*, Select-*, and any command the user pasted verbatim, including Invoke-Command) run immediately with no confirmation. Risky irreversible actions are skipped and listed under "Deferred for safety" — never asked about.
